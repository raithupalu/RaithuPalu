diff a/backend/controllers/orderController.js b/backend/controllers/orderController.js	(rejected hunks)
@@ -42,9 +42,12 @@ exports.placeOrder = async (req, res, next) => {
       totalPrice,
       session: time,
       date: new Date(),
+      entryType: "ORDER",
       notes: `Auto-created from order ${order._id}`,
     });
 
+    console.log(`[DEBUG] MilkEntry created:\nCustomer: ${req.user.username}\nOrder ID: ${order._id}\nentryType: ORDER`);
+
     await Notification.create({
       userId: null,
       message: `New order from ${req.user.username}: ${quantity}L for ${time}`,
@@ -175,6 +178,9 @@ exports.deleteOrder = async (req, res, next) => {
       throw err;
     }
 
+    // Delete corresponding milk entry if it was auto-created from this order
+    await MilkEntry.deleteOne({ notes: `Auto-created from order ${order._id}` });
+
     await Order.findByIdAndDelete(req.params.id);
 
     res.json({ message: "Order deleted successfully" });
